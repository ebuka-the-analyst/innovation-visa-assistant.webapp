import { Card } from "@/components/ui/card"; import { Checkbox } from "@/components/ui/checkbox"; import { AuthHeader } from "@/components/AuthHeader"; import { ToolNavigation } from "@/components/ToolNavigation"; import { useState } from "react"; import { AlertCircle } from "lucide-react";
const ITEMS = [{id:"1",name:"Business Registration",pen:"Critical"},{id:"2",name:"Tax Registration",pen:"High"},{id:"3",name:"Bank Account",pen:"High"},{id:"4",name:"IP Rights",pen:"High"},{id:"5",name:"Evidence Pack",pen:"Critical"},{id:"6",name:"Market Research",pen:"Medium"},{id:"7",name:"Financial Statements",pen:"Critical"},{id:"8",name:"Team Structure",pen:"Medium"}];
export default function ELIGIBILITYVALIDATOR() {
  const [checks, setChecks] = useState<any>({});
  const done = Object.values(checks).filter(Boolean).length, score = Math.round((done/ITEMS.length)*100);
  return <><AuthHeader /><div className="min-h-screen bg-gradient-to-br from-background via-accent/5 to-primary/5 p-6"><ToolNavigation /><h1 className="text-4xl font-bold mb-2">Eligibility Validator</h1><p className="text-muted-foreground mb-6">Compliance checklist with scoring</p><Card className="p-6 mb-6"><div className="flex justify-between items-center mb-4"><span className="text-2xl font-bold">Score: {score}%</span><span className="text-sm">{done}/{ITEMS.length} completed</span></div><div className="w-full bg-gray-200 h-3 rounded-full"><div style={{width:`${score}%`}} className="bg-primary h-3 rounded-full"/></div></Card><div className="space-y-2">{ITEMS.map(i=><Card key={i.id} className={`p-3 border-l-4 ${i.pen==="Critical"?"border-l-red-500 bg-red-50":"border-l-blue-500 bg-blue-50"}`}><label className="flex gap-3 cursor-pointer"><Checkbox checked={checks[i.id]||false} onCheckedChange={()=>setChecks({...checks,[i.id]:!checks[i.id]})}/><div className="flex-1"><p className="font-semibold text-sm">{i.name}</p><p className="text-xs text-muted-foreground">{i.pen}</p></div></label></Card>)}</div></div></>;
}
