import { Card } from "@/components/ui/card"; import { Button } from "@/components/ui/button"; import { Textarea } from "@/components/ui/textarea"; import { AuthHeader } from "@/components/AuthHeader"; import { ToolNavigation } from "@/components/ToolNavigation"; import { useState } from "react";
export default function GTMPLAN() {
  const [data, setData] = useState({s1:"",s2:"",s3:"",s4:""});
  const filled = Object.values(data).filter((v:any)=>v.length>20).length;
  return <><AuthHeader /><div className="min-h-screen bg-gradient-to-br from-background via-accent/5 to-primary/5 p-6"><ToolNavigation /><h1 className="text-4xl font-bold mb-2">Gtm Plan</h1><p className="text-muted-foreground mb-6">Strategy builder</p><Card className="p-4 mb-4"><div className="flex justify-between"><span className="font-bold">Sections</span><span>{filled}/4</span></div><div className="w-full bg-gray-200 h-2 rounded-full mt-2"><div style={{width:`${(filled/4)*100}%`}} className="bg-primary h-2 rounded-full"/></div></Card>{["Summary","Goals","Activities","Metrics"].map((s,i)=><Card key={i} className="p-4 mb-2"><label><span className="font-semibold text-sm block mb-1">{s}</span><Textarea value={data[`s${i+1}`as keyof typeof data]} onChange={e=>setData({...data,[`s${i+1}`]:e.target.value})} className="h-20"/></label></Card>)}<Button className="w-full bg-primary">Export Plan</Button></div></>;
}
