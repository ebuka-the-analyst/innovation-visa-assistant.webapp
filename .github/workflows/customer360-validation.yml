name: Customer 360 Validation

on:
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Check Customer 360 server syntax
        run: |
          node --check server/customer360Admin.cjs
          node --check server/retiredRouteGuard.cjs

      - name: Type-check Customer 360 frontend
        run: |
          cat > .customer360-tsconfig.json <<'JSON'
          {
            "extends": "./tsconfig.json",
            "include": [
              "client/src/components/admin/Customer360Enhancer.tsx",
              "client/src/components/admin/customer360DialogBridge.ts"
            ],
            "exclude": [],
            "compilerOptions": {
              "target": "ES2020",
              "noEmit": true
            }
          }
          JSON
          npx tsc -p .customer360-tsconfig.json

      - name: Production build
        run: npm run build
