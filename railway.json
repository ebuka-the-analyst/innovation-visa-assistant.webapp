{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "preDeployCommand": "node server/scripts/preflight-app-schema.cjs && node server/scripts/run-app-migrations.cjs && node server/scripts/verify-application-context-schema.cjs && node server/scripts/verify-app-schema.cjs",
    "startCommand": "npm run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
